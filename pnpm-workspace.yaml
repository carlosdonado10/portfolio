packages:
  - apps/*
  - packages/*

# pnpm blocks dependency build scripts by default; allow only vetted ones.
# esbuild needs its postinstall to install the platform binary Vite depends on.
allowBuilds:
  esbuild: true
