# @portfolio/notify-client

**PLACEHOLDER — owned by WS5.4.**

This package will hold the notification client protocol and implementation. It
exists now only as a valid pnpm workspace member so the workspace resolves. Do
not build real notify-client code here until WS5.4.
