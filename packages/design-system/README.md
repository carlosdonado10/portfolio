# @portfolio/design-system

**PLACEHOLDER — owned by WS-DS.0.**

This package will hold the shared design tokens and UI primitives (and their
Storybook). It exists now only as a valid pnpm workspace member so the workspace
resolves. Do not build real design-system code here until WS-DS.0.
